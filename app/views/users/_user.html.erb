<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user, id: 'user_name' %>
  <% if current_user.admin? && !current_user?(user) %>
  <div class = 'btn'><%= button_to "Delete", user, method: :delete, data: { confirm: "You sure?" }, class: 'btn' %> </div>
  <% end %>
  <% if @title == "Following" %>
  <% @user = user %>
  <div class = 'btn' ><%= render "unfollow" %></div>
  <% end %>
</li>

<%#= button_to "Add", new_admin_course_path, :id => "open-contacts-dialog-btn",
      :class => "inbox-sf-add-btn tip", :method => :get %>
      
       <%#= link_to "Delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %>


<%#= button_to "Delete", user, method: :delete, data: { confirm: "You sure?"}  %>

 <!--<div class = 'btn'><%#= button_to user.name, user, method: :get, class: 'btn' %></div>-->




